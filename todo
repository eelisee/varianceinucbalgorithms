Heute:

plot 5 einfügen und restliche plots korrigieren

alle algorithmen opt arm mit 0.8 und 0.9 durchlaufen lassen, als _opt_version1 speichern (8,9)
alle algorithmen opt armmit 0.895 und 0.9 durchlaufen lassen, als _opt_verison2 speichern
alle algorithmen opt arm mit 0.495 und 0.5 durchlaufen lassen, als _opt_version 3 speichern

alle algorithmen subopt arm mit 0.8 und 0.9 durchlaufen lassen, als _subopt_version1 speichern
alle algorithmen subopt armmit 0.895 und 0.9 durchlaufen lassen, als _subopt_verison2 speichern
alle algorithmen subopt arm mit 0.495 und 0.5 durchlaufen lassen, als _subopt_version 3 speichern

gaussian distributed arms hinzufügen?

dashboard opt und wahl der algorithmen und arme verknüpfen
graph für actual regret einfügen
_________________________________

BA:

vemrutlich algorithmen vor techniques, dann dashboard direkt in techniques incooperieren.
screenshot dashboard sowie grafiken algorithmen zur auswertung einfügen
dashboard auf englisch übersetzen sowie sonderfälle einfügen


Mittwoch:

calculation nochmal überarbeiten in einem absatz
Simulation of the algorithm: Implementierung der Algorithmen und Besonderheiten erklären + Dashboard theoretisch einführen
    UCB-Normal: fällt aus der Reihe
    UCB-Improved: umformuliert, damit vergleichbar
    Variablen des Dashboards einführen
    Dashboard erklären (Bennennnung dessen was umgesetzt ist), in welchen varianten
exp-exp tradeoff: Dashboard Plots mit Daten beschreiben (für alle fälle)

subopt laufen lassen, leif fragen was mit related work

Donnerstag: vorschrieb überarbeiten, Leif Leseprobe schicken
